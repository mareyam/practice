import React from "react";
import { Card, CardHeader, Text, CardBody, CardFooter } from "@chakra-ui/react";
const Billing = () => {
  return (
    <Card>
      <CardBody>
        <Text>View a summary of all your customers over the last month.</Text>
      </CardBody>
    </Card>
  );
};

export default Billing;
